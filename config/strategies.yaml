monitored_assets:
  BNBUSDT: testnet
  MKRUSDT: testnet
  LTCUSDT: testnet
  XRPUSDT: testnet

strategies:
  BCHUSDT:
    strategy: orbit.strategies.reversal_strategy.BollingerAdaptiveReversalStrategyBCH
    execution_mode: testnet

  ETHUSDT:
    strategy: orbit.strategies.eth_strategy.ETHStrategy
    execution_mode: testnet

  BTCUSDT:
    strategy: orbit.strategies.btc_strategy.BTCStrategy
    execution_mode: testnet

  PAXGUSDT:
    strategy: orbit.strategies.paxgusdt_strategy.PAXGUSDTStrategy
    execution_mode: testnet

  SKYUSDT:
    strategy: orbit.strategies.skyusdt_strategy.SKYUSDTStrategy
    execution_mode: testnet

  ATOMUSDT:
    strategy: orbit.strategies.atomusdt_strategy.ATOMUSDTStrategy
    execution_mode: testnet

  SOLUSDT:
    strategy: orbit.strategies.solusdt_strategy.SOLUSDTStrategy
    execution_mode: testnet
